import type { Metadata } from 'next';

import { ContinueTopic } from '@/components/topic/ContinueTopic';
import { TopicsCatalog } from '@/components/topic/TopicsCatalog';

export const metadata: Metadata = {
  title: 'Правила',
  description:
    'Усі теми граматики англійської з поясненнями українською: часи, артиклі, умовні речення, модальні дієслова, прийменники. Короткі розділи, приклади з перекладом, вправи й тест.',
};

/**
 * Каталог правил.
 *
 * Сторінка називається «Правила», а не «Теми», бо так її називає й
 * навігація: одне слово на пункт меню, заголовок і адресний рядок
 * розмови («де це? у правилах»). Маршрут лишився `/topics` — міняти
 * його заради синоніма означало б покласти редирект і зламати всі
 * зовнішні посилання на теми заради нічого.
 *
 * Сама сторінка серверна й порожня: увесь вміст — це дані з
 * `src/data/topics.ts`, а два клієнтські острівці під нею тримають
 * рівно те, що без браузера не існує, — фільтр рівня і прогрес
 * читача.
 */
export default function TopicsPage() {
  return (
    /* leading-[1.5] — метрика макета, як і на головній. `body` несе 1.6
       заради довгого читання правил, але каталог не читають абзацами:
       на ліді в 16px різниця між 1.6 і 1.5 дає 3px на два рядки, і вся
       полиця під ним з'їжджає вниз. */
    <div className="mx-auto w-full max-w-shell px-10 pt-11 pb-16 leading-[1.5]">
      {/* «Продовжити» стоїть слотом усередині каталогу, а не перед ним:
          у макеті вона між заголовком і полицею, а заголовок несе фільтр
          рівня — тобто клієнтський стан. Витягнути заголовок сюди значило
          б розірвати фільтр і сітку, якими він керує. */}
      <TopicsCatalog continueSlot={<ContinueTopic />} />
    </div>
  );
}
