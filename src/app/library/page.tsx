import { redirect } from 'next/navigation';

/**
 * `/library` переїхала на `/reading`: полиця тепер показує не лише готові
 * оповідання, а й власні тексти, і «бібліотека» перестала бути точною назвою
 * для того, що на ній лежить.
 *
 * Сам маршрут лишається переспрямуванням, а не видаленим: на `/library`
 * стоять зовнішні посилання й закладки читачів, і 404 замість полиці був би
 * єдиним, що вони побачать. Читалка `/library/<slug>` не змінилася — сюди
 * потрапляє тільки корінь розділу.
 */
export default function LibraryPage() {
  redirect('/reading');
}
